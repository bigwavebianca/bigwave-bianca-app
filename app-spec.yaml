name: bigwave-bianca-app
region: nyc
services:
  - name: bigwave-bianca
    image:
      registry_type: DOCKER_HUB
      registry: library
      repository: nginx
      tag: alpine
    http_port: 80
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    routes:
      - path: /
